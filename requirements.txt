mlx>=0.31.0
mlx-vlm>=0.5.0
safetensors>=0.7.0
transformers>=5.0.0
numpy
huggingface_hub

# Optional, for benchmarks/quality (CUDA/vLLM quality benchmarks):
#   pip install "edge-lm[quality]"
# Optional, for examples/chat.py --voice (microphone input):
#   pip install "edge-lm[voice]"  or   pip install sounddevice>=0.5.0
