mlx>=0.31.0
mlx-vlm>=0.5.0
safetensors>=0.7.0
transformers>=5.0.0
numpy
huggingface_hub

# Optional, for benchmarks/evaluate.py (quality benchmarks via lm-evaluation-harness):
#   pip install "edge-lm[eval]"   or   pip install lm-eval>=0.4.12
# Optional, for examples/chat.py --voice (microphone input):
#   pip install "edge-lm[voice]"  or   pip install sounddevice>=0.5.0
